import 'package:flutter/material.dart';

class ProfileBtnW extends StatelessWidget {
  const ProfileBtnW({super.key});

  @override
  Widget build(BuildContext context) {
    return TextButton(
      onPressed: () {},
      child: Icon(Icons.account_circle, size: 25, color: Colors.black),
    );
  }
}
